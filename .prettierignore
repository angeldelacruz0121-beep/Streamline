# Prettier's scope is source code only. Governance docs, decision records and
# agent files are hand-formatted and owned elsewhere - reflowing them would
# produce cross-boundary diffs no one asked for.
*.md
.claude/
docs/
reports/

# Retired prototype - frozen by docs/decisions/0001, never reformatted.
reference/

# Approved verbatim by Angel; kept byte-exact.
package.json

dist/
coverage/
package-lock.json

# Verbatim captures of the live proxy's response bytes, used as adapter test
# fixtures. Reformatting them would break the one property that makes them
# trustworthy: that they are exactly what the server sent (Invariant 4.5).
src/app/sources/fixtures/

# QA Engineer's adversarial corpus - same reasoning, same invariant. These are
# the evidence the release gate is judged against; a format pass silently
# destroys the byte-exactness that makes them evidence at all.
fixtures/envelopes/
fixtures/verified/
